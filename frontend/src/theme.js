import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#0000'//'#0b3c91',
    },
    secondary: {
      main: '#1565c0',
    },
    text: {
      primary: '#ffffff',
    },
  },
});

export default theme;