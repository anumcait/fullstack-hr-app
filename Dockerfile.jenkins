FROM jenkins/jenkins:lts

USER root

# Install Docker CLI and Compose Plugin
RUN apt-get update && \
    apt-get install -y docker.io && \
    apt-get clean

# optional: prove docker works
RUN docker --version

USER jenkins
